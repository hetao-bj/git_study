{
    "tasks": [
        {
            "label": "tvmbuild",
            "type": "shell",
            "command": "cmake --build ./build --config Debug --target all -- -j22",
            "group": {
                "kind": "build",
                "isDefault": true
            }
        }
    ],
    "version": "2.0.0"
}